# 统一换行符，避免每次发布产生无意义的整文件 diff
* text=auto eol=lf

# Windows 批处理必须 CRLF，否则 cmd 解析可能出错
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制不做换行转换
*.docx binary
*.xlsx binary
*.pdf  binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
